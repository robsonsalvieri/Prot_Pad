#INCLUDE "PROTHEUS.CH"
#INCLUDE "RSPXRDM.CH"

Function RSPXRDM()
Return

Function RSPXIMPCAN()

If ExistBlock("IMPRCAN")
    ExecBlock( "IMPRCAN", .F., .F. )
Else
	RSPR010()
EndIf

Return

Function RSPXRSR001()

If ExistBlock("RSR001")
    ExecBlock( "RSR001", .F., .F. )
Else
	RSPR020()
EndIf

Return

Function RSPXRSR003()

If ExistBlock("RSR003")
    ExecBlock( "RSR003", .F., .F. )
Else
	RSPR030()
EndIf

Return

Function RSPXRSR004()

If ExistBlock("RSR004")
    ExecBlock( "RSR004", .F., .F. )
Else
   RSPR040()
EndIf

Return
