#INCLUDE "PROTHEUS.CH"
#INCLUDE "TRMXRDM.CH"

Function TRMXRDM()
Return

Function TRMXCERT()
If ExistBlock("CERT")
    ExecBlock( "CERT", .F., .F. )
Else
   TRMR110()
EndIf
Return

Function TRMXCONVIT()
If ExistBlock("CONVIT")
    ExecBlock( "CONVIT", .F., .F. )
Else
   TRMR120()
EndIf
Return

Function TRMXTRM010()
If ExistBlock("TRM010")
    ExecBlock( "TRM010", .F., .F. )
Else
    TRMR130()
EndIf
Return

Function TRMXTRM020()
If ExistBlock("TRM020")
    ExecBlock( "TRM020", .F., .F. )
Else
    TRMR140()
EndIf
Return

Function TRMXTRM030()
If ExistBlock("TRM030")
    ExecBlock( "TRM030", .F., .F. )
Else
    TRMR150()
EndIf
Return

Function TRMXTRM040()
If ExistBlock("TRM040")
    ExecBlock( "TRM040", .F., .F. )
Else
    TRMR160()
EndIf
Return

Function TRMXTRM060()
If ExistBlock("TRM060")
    ExecBlock( "TRM060", .F., .F. )
Else
   TRMR170()
EndIf
Return

Function TRMXTRM070()
If ExistBlock("TRM070")
    ExecBlock( "TRM070", .F., .F. )
Else
	TRMR180()
EndIf
Return

Function TRMXTRM080()
If ExistBlock("TRM080")
    ExecBlock( "TRM080", .F., .F. )
Else
	TRMR190()
EndIf
Return
