#INCLUDE "PROTHEUS.CH"
#INCLUDE "GPEXRDM.CH"

Function GPEXRDM()
Return

Function GPEXBENEF()

If ExistBlock("BENEFARQ")
    ExecBlock( "BENEFARQ", .F., .F. )
Else
    GPEM931()
EndIf

Return

Function GPEXETIQAS()

If ExistBlock("ETIQAS")
    ExecBlock( "ETIQAS", .F., .F. )
Else
    GPER981()
EndIf

Return

Function GPEXETIQCT()

If ExistBlock("ETIQCT")
    ExecBlock( "ETIQCT", .F., .F. )
Else
    GPER982()
EndIf

Return

Function GPEXETIQFE()

If ExistBlock("ETIQFER")
    ExecBlock( "ETIQFER", .F., .F. )
Else
    GPER983()
EndIf

Return

Function GPEXETIQMD()

If ExistBlock("ETIQMD")
    ExecBlock( "ETIQMD", .F., .F. )
Else
    GPER984()
EndIf

Return

Function GPEXETIQOP()

If ExistBlock("ETIQOPC")
    ExecBlock( "ETIQOPC", .F., .F. )
Else
    GPER985()
EndIf

Return

Function GPEXRPPP()

If ExistBlock("GPERPPP")
    ExecBlock( "GPERPPP", .F., .F. )
Else
    GPER986()
EndIf

Return

Function GPEXWORD()

If ExistBlock("GPEWORD")
    ExecBlock( "GPEWORD", .F., .F. )
Else
    GPER987()
EndIf

Return

Function GPEXIMPCAT()

If ExistBlock("IMPCAT")
    ExecBlock( "IMPCAT", .F., .F. )
Else
    GPER988()
EndIf

Return

Function GPEXIMPMOD()

If ExistBlock("IMPMODEL")
    ExecBlock( "IMPMODEL", .F., .F. )
Else
    GPER998()
EndIf

Return

Function GPEXIMPRPA()

If ExistBlock("IMPRPA")
    ExecBlock( "IMPRPA", .F., .F. )
Else
    GPER1003()
EndIf

Return

Function GPEXREAJTA()

If ExistBlock("REAJTAR")
    ExecBlock( "REAJTAR", .F., .F. )
Else
	GPER1004()
EndIf

Return

Function GPEXSEGDES()

If ExistBlock("SEGDES")
    ExecBlock( "SEGDES", .F., .F. )
Else
	GPEM935()
EndIf

Return

Function GPEXRECBRA()

If ExistBlock("RECBRAD")
    ExecBlock( "RECBRAD", .F., .F. )
Else
    GPEM937()
EndIf

Return

Function GPEXRECITA()

If ExistBlock("RECITAU")
    ExecBlock( "RECITAU", .F., .F. )
Else
   GPEM933()
EndIf

Return

Function GPEXRECSAN()

If ExistBlock("RECSANT")
    ExecBlock( "RECSANT", .F., .F. )
Else
    GPEM934()
EndIf

Return
