#include "Totvs.ch"
#include "tlpp-core.th"

/*/{Protheus.doc} WMSSaasManufaturaItemApontamentoProducao
    Classe de Item Manufatura Requisicao
    @type class
    @author Roselaine Adriano
    @since 30/01/2024
    /*/
Class WMSSaasManufaturaItemApontamentoProducao From WMSSaasItemConvergencia
    Public Method set(cProduto as character, nQuantidade as numeric, cArmazem as character, cLote as character, dDataValidade as date, cNumSeq as character) as object
EndClass

/*/{Protheus.doc} set
    @author Roselaine Adriano
    @since 30/01/2024
    @version 1.0
    @param cProduto, character, Produto
    @param nQuantidade, decimal, Quantidade
    @param cArmazem, character, Armazém
    @param cLote, character, Lote
    @param dDataValidade, date, Data Validade
    @return Self, object, Objeto tipo ItemConvergencia    
    /*/
Method set(cProduto, nQuantidade, cArmazem, cLote, dDataValidade, cNumSeq) Class WMSSaasManufaturaItemApontamentoProducao as object
    self:produto         := cProduto
    self:quantidade      := nQuantidade
    self:armazem         := cArmazem
    self:lote            := cLote
    self:dataValidade    := dDataValidade
    self:numSeq          := cNumSeq
Return self
