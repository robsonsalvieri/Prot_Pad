#Include "TOTVS.ch"

/*/{Protheus.doc} GPERSV630
    Chamada dos artefatos do objeto de negócio rh.treports.gper630 (Folha de Rescisões) do Smart View.
    @type Function
    @version 12.1.2410
    @author arthur.sales
    @since 12/12/2024
    @return Variant, Retorno nulo pré-fixado
/*/
Function GPERSV630()
    // Declaração das variáveis locais
    Local oSmartView As Object  // Instância da classe callSmartView()

    // Inicialização das variáveis
    oSmartView := NIL

    // Valida a release do Protheus
    If (GetRpoRelease() >= "12.1.2310")
        // Instancia a classe callSmartView()
        oSmartView := totvs.framework.smartview.callSmartView():new("rh.sv.gpe.gper630")
        oSmartView:setParam("IMPMEDIA", "2")
        oSmartView:setShowWizard(.T.)

        // Verifica se ocorreu algum erro na execução do artefato do Smart View
        If (!oSmartView:executeSmartView(.T.))
            ConOut(oSmartView:getError())
        EndIf

        // Destrói o objeto e o libera da memória
        oSmartView:destroy()
        FwFreeObj(oSmartView)
    EndIf
Return NIL
