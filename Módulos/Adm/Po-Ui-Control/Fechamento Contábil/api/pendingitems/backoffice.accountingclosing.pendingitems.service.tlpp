#include "tlpp-core.th"

namespace totvs.protheus.backoffice.accountingclosing.pendingitems

//-------------------------------------------------------------------
/*/{Protheus.doc} PendingItemsService
Classe de serviço, realizada instância e chamada de métodos de serviços

@author Totvs
/*/
//-------------------------------------------------------------------
class PendingItemsService
    public method new()
    public method getInstance()
    public method getAllPendingItems()
endclass

//-------------------------------------------------------------------
/*/{Protheus.doc} PendingItemsService
Metodo construtor

@author Totvs
/*/
//-------------------------------------------------------------------
method new() class PendingItemsService
return

//-------------------------------------------------------------------
/*/{Protheus.doc} PendingItemsService
Responsável por instanciar a classe controller

@author Totvs
/*/
//-------------------------------------------------------------------
method getInstance() class PendingItemsService
    static __instance As Object
    
    If ValType(__instance) == "U"
        __instance := PendingItemsService():new()
    EndIf
return

//-------------------------------------------------------------------
/*/{Protheus.doc} getAllPendingItems
Responsável por chamar a consulta e parsear o retorno no formato JSON

@author Totvs
/*/
//-------------------------------------------------------------------
method getAllPendingItems(jBody as Json) class PendingItemsService
    Local oSettings := PendingItemsProtheusData():getData() As Object
    Local cReturn   := "" As Character

    cReturn := oSettings:getAll(jBody)
    
    oSettings:DeActivate()
    FreeObj(oSettings)
return cReturn
