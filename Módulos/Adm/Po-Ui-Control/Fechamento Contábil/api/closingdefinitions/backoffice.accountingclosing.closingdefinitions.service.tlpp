#include "tlpp-core.th"

namespace totvs.protheus.backoffice.accountingclosing.closingdefinitions

//-------------------------------------------------------------------
/*/{Protheus.doc} ClosingDefinitionsService
Classe de serviço, realizada instância e chamada de métodos de serviços

@author Totvs
/*/
//-------------------------------------------------------------------
class ClosingDefinitionsService
    public method new()
    public method getInstance()
    public method getAllClosingDefinitions()
endclass

//-------------------------------------------------------------------
/*/{Protheus.doc} ClosingDefinitionsService
Metodo construtor

@author Totvs
/*/
//-------------------------------------------------------------------
method new() class ClosingDefinitionsService
return

//-------------------------------------------------------------------
/*/{Protheus.doc} ClosingDefinitionsService
Responsável por instanciar a classe controller

@author Totvs
/*/
//-------------------------------------------------------------------
method getInstance() class ClosingDefinitionsService
    static __instance As Object
    
    If ValType(__instance) == "U"
        __instance := ClosingDefinitionsService():new()
    EndIf
return

//-------------------------------------------------------------------
/*/{Protheus.doc} getAllClosingDefinitions
Responsável por chamar a consulta e parsear o retorno no formato JSON

@author Totvs
/*/
//-------------------------------------------------------------------
method getAllClosingDefinitions(nPage as Numeric, nPageSize as Numeric) class ClosingDefinitionsService
    Local oSettings := ClosingDefinitionsProtheusData():getData() As Object
    Local jResponse := JsonObject():new() As Json

    oSettings:getAll(nPage, nPageSize)

    IIf(oSettings:lOk,; 
            jResponse := oSettings:getJSONResponse(),; 
            jResponse := SetRestFault(oSettings:GetCode(), oSettings:getMessage()))
    
    oSettings:DeActivate()
    FreeObj(oSettings)
return jResponse
